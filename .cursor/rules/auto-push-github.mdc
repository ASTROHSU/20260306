---
description: 完成編輯後自動推送到 GitHub
alwaysApply: true
---

# 自動推送到 GitHub

在此專案中完成任何檔案編輯後，請自動執行以下步驟：

1. `git add` 已修改的檔案
2. `git commit` 並使用簡潔的 commit message（例如：feat: 描述變更、fix: 修復問題）
3. `git push` 推送到 origin

無需詢問使用者是否要推送，直接執行即可。
