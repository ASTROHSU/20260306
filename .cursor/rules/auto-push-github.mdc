---
description: 只在使用者要求時才推送到 GitHub
alwaysApply: true
---

# 手動推送到 GitHub

在此專案中**不要自動**幫使用者執行 `git add`、`git commit`、`git push`。

- 如需推送到 GitHub，**只在使用者明確開口要求**（例如「幫我更新 GitHub」）時再執行。
- 其他情況僅修改檔案即可，保持本機未必同步到遠端沒關係。
